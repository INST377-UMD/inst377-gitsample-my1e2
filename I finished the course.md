Process to follow in terminal commands:
•	Git clone followed by URL of the remote repo or the SSH key copied from the repo
•	Git status  Git add followed by file (followed by dot if already inside cloned local repo file)
Git status again to check everything  Git commit -m followed by conventional commit and quoted message
Git status again to check stuff  Git push to save changes to remote repo 
